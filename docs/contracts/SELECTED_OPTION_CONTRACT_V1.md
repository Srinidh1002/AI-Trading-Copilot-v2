# Selected Option Contract v1

Selection maps BUY to CALL and SELL to PUT using supplied contracts only. Earliest expiry and deterministic ATM tie-break policies are supported; a selection is blocked when no eligible contract exists.
