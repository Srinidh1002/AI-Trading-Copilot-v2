# RiskPolicyV1

`RiskPolicyV1` is the immutable P3-5A validation-only policy schema. It
contains capital and risk ceilings, required price controls, and deterministic
insufficient-capital behavior. It performs no position-size calculation.
