[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-trading-copilot-v2"
version = "0.1.0"
description = "AI Trading Copilot"
dependencies = []